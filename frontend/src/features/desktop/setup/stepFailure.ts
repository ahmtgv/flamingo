export { failureText as stepFailureKey } from '@/shared/lib/requestFailure';

/**
 * Почему шаг мастера не прошёл — теперь общими словами продукта.
 *
 * 🔴 Находка владельца 15.08: на шаге 2 кнопка «Дальше» нажималась и не делала НИЧЕГО — ни
 * перехода, ни ошибки. Тот же дефект был на шагах 3, 4 и «Готово»: они написаны одинаково и
 * одинаково молчали.
 *
 * ⚠️ Аудит 16.08 увёл сами строки в `shared/lib/requestFailure`: отказ объяснялся в трёх местах
 * тремя разными наборами слов, а сдача домашней работы вообще одной фразой на любую причину.
 * Здесь остаётся только имя, под которым мастер к ним обращается.
 */
