/**
 * Куда возвращает знак бренда и кнопка «В кабинет».
 *
 * 🔴 ВЫЙДЯ СО СТАРТОВОЙ ОДИН РАЗ, ВЕРНУТЬСЯ НА НЕЁ БЫЛО НЕЧЕМ (аудит продукта 17.08).
 *
 * Одиннадцать мест — логотипы и «в кабинет» на `/courses`, `/schedule`, `/grading`,
 * `/homework`, `/admin` — вели на `/app`, то есть в кабинет ПРЕЖНЕЙ сборки. Стартовая страница
 * по листу 00 и есть кабинет; человек уходил с неё в расписание, нажимал «В кабинет» и попадал
 * на другой экран с другим устройством, из которого назад дороги уже нет. Это и есть смесь в
 * самом дорогом виде: не «экран старый», а «новый и старый склеены переходом».
 *
 * ⚠️ Старый кабинет НЕ УДАЛЁН и удалён не будет — это решение владельца. На нём держатся два
 * ролевых кабинета целиком: родитель и админ учреждения. Их `StartScreen` отправляет на `/app`
 * сам, одной строкой и осознанно. Поэтому здесь безопасно указать `/start` для всех: у кого
 * стартовой нет, того она же и переадресует туда, где его кабинет.
 */
export const HOME_ROUTE = '/start';
