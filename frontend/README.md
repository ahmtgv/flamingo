# frontend

React + TypeScript + Vite. Два экрана: вход и комната.

```bash
npm install
npm run dev     # http://localhost:5173
npm run check   # типы + сборка
```

Порядок работы и границы куска — в корне: `../CLAUDE.md`, `../docs/ГРАНИЦА.md`.
