# ВОПРОСЫ ВЛАДЕЛЬЦУ — открытые, ждут решения

Сюда попадает то, что исполнитель **не имеет права решать сам**: не дефект, а развилка.

---

## 1. Доска общая на все проведения урока (наряд 47 §5, 23.08.2026)

**Как устроено сейчас.** `backend/apps/board/models.py:21` — `lesson = OneToOneField(...)`.
**Одна доска на урок**, слова «занятие»/`session` в приложении `board` не встречаются ни разу.
Комментарий в `LiveRoomScreen` говорит прямо: «доска принадлежит уроку, а не занятию — она
переживает свои проведения».

**Что из этого следует.** Второе занятие того же урока открывает холст первого: со штрихами,
надписями и картинками прошлой группы. Очистки доски нет ни в одной мутации — только
поштучное удаление; `MAX_ELEMENTS = 2000` показывает, что накопление задумано как норма.

**Вопрос.** Так задумано — или каждому занятию нужен чистый холст, а прошлый оставлять
в конспекте?

⚠️ Своей волей не менял: это решение о смысле, а не о вёрстке. Сделан только минимум,
названный в наряде, — порог штриха поднят, чтобы случайное движение мышью не пачкало урок
навсегда (`BoardCanvas.tsx: isRealStroke`).
