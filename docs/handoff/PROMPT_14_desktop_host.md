# ПРОМПТ №14 — Р5: ДЕСКТОП-ХОСТ. УРОК ИДЁТ С МАШИНЫ ПРЕПОДАВАТЕЛЯ

**Кому:** Claude Code. **Ревьюер** сверяет по git и вживую. **Владелец** — Адель.
**Статус:** активная фаза после R4. `PROMPT_13_release_v1.md` остаётся в силе как описание продукта
и как список инвариантов — этот промпт его **не заменяет**, а продолжает: там веб, здесь то,
что делает урок независимым от арендованного сервера.

**Решение владельца 13.08.2026 (OWNER_SCOPE §18, `R5_DESKTOP_HOST_BUDGET.md`):**
вариант 1 — **десктопное приложение преподавателя + ученик в браузере**; десктоп у ученика — опция.
Сервер остаётся только как **точка встречи + сигналинг + TURN** (≈1 700–2 800 ₽/мес против 11 000–13 000).
Полный серверный контур **не выбрасывается**: он написан и остаётся запасным путём.

---

## 0. ПОЛНОМОЧИЯ

Ты работаешь как: **архитектор десктоп-приложения** · **backend Django/Strawberry** ·
**инженер real-time (WebRTC, сигналинг, TURN)** · **frontend React/TS** · **инженер по приватности** ·
**релиз-инженер**.

Можно: заводить новые приложения Django, расширять SDL вручную, вводить конфигурацию через переменные
окружения, добавлять десктопную сборку и её инфраструктуру.

Нельзя без возврата к владельцу: трогать инварианты приватности и хранения, вводить платные лицензии
и подписи установщиков (решение 14.08 — **подпись отложена**), включать отложенные направления,
реализовывать листы атласа, которые владелец ещё не утвердил.

---

## 1. ЧТО ДОЛЖНО РАБОТАТЬ В КОНЦЕ Р5

Один маршрут, целиком, на двух реальных преподавателях:

1. Преподаватель скачивает приложение с сайта, ставит, **связывает машину кодом через браузер**.
2. Приложение говорит, где лежит кабинет, настраивает **обязательную копию**, собирает согласия,
   **меряет канал** и называет посильный размер группы.
3. Преподаватель зовёт учеников **постоянной ссылкой группы** (или кодом).
4. Ученик открывает ссылку в браузере: ждёт, видит расписание, проверяет камеру и звук —
   и **страница сама открывает комнату**, когда занятие началось.
5. Урок идёт **с машины преподавателя**: медиа через WebRTC напрямую, через TURN — когда сеть не пускает прямое.
6. Преподавателя нет в сети — ученик видит **честный экран**, а не ошибку: когда занятие,
   что можно делать сейчас, что недоступно и почему.
7. Кабинет **выгружается одним файлом** и из него же восстанавливается.

---

## 2. 🔒 «НЕ НАВРЕДИ» — ПРОВЕРЯЕТСЯ НА КАЖДОЙ ФАЗЕ

**В каждом отчёте обязателен раздел «что из работающего могло пострадать и чем доказано, что цело».**

### 2.1 Инварианты, которые эта фаза не имеет права ослабить
- **Приватность камеры** (CLAUDE.md §2.1): эгресс SEduM = 8 агрегатных скаляров, в БД только `avg_attention`,
  сырых кадров/ландмарок нет нигде. `test_privacy.py`.
- **Правило хранения** (CLAUDE.md §2.2): видео, аудио и дословная расшифровка не хранятся **никогда**,
  включая локальную БД на машине преподавателя. `test_storage_policy.py`.
  ⚠️ Локальное хранилище — не лазейка. «На своём компьютере» не отменяет запрет.
- **Юрисдикционный гейт**: единственная точка решения `common/compliance/policy.py`, никаких страновых `if`
  вне PDP. Каждая новая фича десктопа регистрируется в `matrix.json` с `legal_basis_ref`.
- **Пер-резолверная авторизация** и `courses/access.py: can_access_course`. Локальный режим **не даёт**
  права ослабить проверки: приложение слушает на 127.0.0.1, но авторизация остаётся серверной логикой.

### 2.2 Новые инварианты этой фазы (завести тесты вместе с кодом)
1. **Приложение никогда не принимает пароль.** Ни поля, ни эндпоинта, ни передачи пароля в sidecar.
   Вход — только обмен разового кода на токен машины (решение владельца 14.08, OWNER_SCOPE §19.4).
   Тест: в десктопном слое нет ни одного пути, где строка пароля пересекает границу процесса.
2. **Токен машины хранится в связке ключей ОС**, не в файле конфигурации и не в localStorage.
3. **Код связывания одноразовый и живёт 10 минут.** Тест на повторное использование и на истечение.
4. **Экспорт кабинета содержит ровно `CABINET_TABLES`** (`common/portability.py`) и ничего из `NOT_EXPORTED`.
   Тест сравнивает состав выгрузки со списком, а не с ожиданиями автора.
5. **Копия, уходящая наружу, зашифрована на клиенте.** Сервер видит непрозрачный блоб + `keyHint` —
   тот же контракт, что у `UbpBackup`.

### 2.2-бис 🔒 Не изобретать ограничений (установка владельца 14.08, OWNER_SCOPE §20.4.3)
Продукт по умолчанию **открытый и полный**. Ограничение вводится только если оно (а) решение владельца
или (б) строка закона со ссылкой. Единственное место для него — юрисдикционный гейт
(`common/compliance/policy.py` + `matrix.json` с `legal_basis_ref`), никогда не `if` в резолвере
и не «на всякий случай выключим».

Неприкосновенным остаётся то, что владелец решил сам: запрет на видео/аудио/расшифровку занятия,
инвариант приватности камеры, обязательная копия кабинета, пароль не вводится в приложении.
**Сомневаешься между «запретить на всякий случай» и «спросить» — спрашивай.** Молчаливый запрет
дороже вопроса: он тихо становится продуктом.

### 2.3 Гейты
База после **Р5.0 + Р5.0-Б** (14.08): **430 pytest**, **370 vitest**, `ruff` + `black` + `eslint` +
**`npm run build`** — зелёные. *(До фазы было 375, а не 373: цифра в первой редакции устарела на две
правки. Считает исполнитель, спорить не о чем — фиксируем 430/370 как новую базу.)*
Каждая фаза сообщает числа **до → после**. Падение существующего теста = стоп, а не «поправим потом».
⚠️ Считать тесты только по `backend/apps` + `backend/tests` и `frontend/src` — **не по `.venv`**.
⚠️ Гейт фронтенда — `npm run build`, не `tsc --noEmit`.

### 2.4 Дизайн-контракт
Реализуются **только утверждённые листы**. На старте Р5 утверждены и закреплены решениями владельца:
- **D1** `D1_app_frame.html` — рама, состояние хоста, окно «Класс», связь словами, переключатели в полосе состояния.
- **D2** `D2_first_run.html` — пять шагов + настройки; решения 14.08 записаны в OWNER_SCOPE §19.
- **D3** `D3_invite_and_join.html` — утверждён, решения владельца 14.08 записаны в OWNER_SCOPE §20:
  по умолчанию «только ученики группы», зеркало материалов ученика делаем **сейчас**,
  **данные ученика принадлежат ученику навсегда**.
Веб-листы 00 · 01 · 02 · 12 остаются контрактом: те же экраны живут внутри десктопного окна.
`_archive/` — не контракт.

---

## 3. ФАЗЫ

Порядок обязателен: каждая следующая опирается на предыдущую. Внутри фазы — свободен.

### Р5.0 — Точка встречи и связывание машины (backend, без десктопа)
Самая ценная фаза: после неё ученик перестаёт зависеть от машины преподавателя в тех местах, где не должен.

- Новое приложение `apps/devices`: `DEVICE` (машина преподавателя), `PAIRING_CODE`, `DEVICE_TOKEN`.
  Мутации: `requestPairingCode` (из приложения) · `confirmPairingCode` (из браузера, под сессией пользователя) ·
  `revokeDevice`. Запрос `myDevices`.
- Новое приложение `apps/meetingpoint` (или расширение `scheduling`): **постоянная ссылка группы** + код,
  режим доступа (`group_only` | `any_authenticated` | `knock`), `presence` преподавателя (в сети / нет),
  расписание, читаемое **без** машины преподавателя.
- Подписка `hostPresenceChanged` — ею живёт экран «преподаватель не в сети» и авто-открытие комнаты.
- Демо-проекция для каждого нового запроса (§2.5 промпта 13) — иначе витрина `flamingo.plus` встанет.

**Приёмка:** тесты на одноразовость и истечение кода, на отзыв устройства, на то, что расписание
и чат доступны при офлайне хоста.

### Р5.0-Б — 🔒 Зеркало ученика (решение владельца 14.08, OWNER_SCOPE §20.3)
**Новая обязательная часть Р5.0.** Данные ученика принадлежат ученику **навсегда**: уход преподавателя
с платформы не имеет права отобрать у ребёнка его учёбу. Это не настройка — это вторая точка хранения.

**Граница собственности — одно правило, без исключений: выдал классу → стало общим и появилось
у ученика; не выдал → своё.**

| Кому | Что зеркалится в точку встречи |
|---|---|
| **Ученику** | его работы, все попытки и пересдачи · оценки, прогресс, достижения · **саммари занятий, на которых он был** (с чатом занятия внутри) · снимок доски, попавший в саммари · его чаты |
| **Преподавателю** | ничего из этого списка не забирается: программы, методички, личные заготовки досок и невыданные материалы остаются на его машине |

Требования:
- зеркалирование **по факту события** (саммари собрано · работа проверена · оценка выставлена),
  а не пакетной выгрузкой по расписанию;
- у каждой записи устойчивый идентификатор и время изменения — уже заложено в `common/portability.py`,
  переиспользовать, не изобретать заново;
- доступ ученика к зеркалу проходит **те же пер-резолверные проверки**, что и в вебе; чужое зеркало не видно;
- **никакого медиа занятия** — запрет CLAUDE.md §2.2 (видео, аудио, дословная расшифровка) действует
  на **обе** точки хранения одинаково. Но **работа ребёнка принадлежит ребёнку целиком**:
  файлы, приложенные учеником к своей работе (сочинение, фото решения, аудио с чтением вслух),
  зеркалятся **содержимым** и открываются всегда — решение владельца 14.08, OWNER_SCOPE §20.4.1.
  Ограничение по размеру файла — забор от медиа урока, а не экономия. Материалы, выданные
  преподавателем, на первом шаге — имя и ссылка;
- **одна попытка = одна запись** со всеми ответами внутри (§20.4.2). Пооперационных записей
  на каждое нажатие не заводить: ученику нужен документ, а не журнал событий;
- хранение в РФ (152-ФЗ), состав — в пределах белого списка PROMPT_13 §4.2;
- сервер **не становится медиасервером**: Р5.2 (медиа с машины преподавателя) не отменяется.

**Приёмка:** тест «преподаватель отвязал машину / удалил аккаунт → ученик открывает свои работы,
оценки и саммари»; тест границы собственности (невыданная методичка в зеркале **не появляется**);
`test_storage_policy.py` расширен на зеркало.

### Р5.1 — Сигналинг, TURN, замер канала
- Сигналинг поверх Channels (WS): обмен SDP/ICE, комната = занятие. Медиа через сервер **не идёт**.
- `infra/`: coturn в docker-compose, конфигурация через env, короткоживущие TURN-креденшелы (HMAC).
- Замер исходящего канала: 12 секунд, результат → **вердикт словами** и посильный размер группы
  (1,1 / 2,4 / 4,3 Мбит/с на 2 / 4 / 8 участников — числа из `R5_DESKTOP_HOST_BUDGET.md` §3).
- Тип соединения (`direct` | `relay`) виден в настройках и в полосе состояния.

**Приёмка:** два браузера на разных машинах соединяются напрямую; при закрытом UDP — через TURN,
и это видно в интерфейсе. Замер повторяется автоматически за 5 минут до занятия.

⚠️ **Живая приёмка Р5.1 переносится в Р5.3/Р5.4** — она требует клиента, которого на момент Р5.1
не существует. Исполнитель её **не имитировал и чисел не выдумывал** — это правильное поведение,
зафиксировано. Проверяется на **машинах двух реальных преподавателей**, а не в лаборатории:
прямое соединение · соединение через TURN · исходящий битрейт при восьми учениках ·
битрейт с подключённым планшетом (Р5.7) — он обязан не измениться.

### Р5.2 — Упаковка: Tauri + локальная база
⚠️ **Начать с ADR, а не с кода.** `docs/adr/ADR-001-desktop-runtime.md`: как внутри приложения живёт backend.
Рабочая гипотеза (её и проверяй первой): **Django как sidecar-процесс на SQLite**, слушает 127.0.0.1
с разовым токеном, React грузится из бандла. Причина: переиспользуются модели, сервисы, права и все тесты —
именно на этом строилась экономика решения (`R5_DESKTOP_HOST_BUDGET.md` §5).

Спайк обязан ответить на три вопроса и показать код, а не рассуждение:
1. **Мигрируются ли миграции на SQLite** — нативные enum, JSONB, UUID PK. Что не переносится — как обходим.
2. **Вес и старт**: размер установщика, время холодного запуска.
3. **Обновление** — как обновляется sidecar вместе с оболочкой (детали — D6, но путь наметить сейчас).

Если гипотеза не выдержит — предложи альтернативу с цифрами и **вернись к владельцу**, не переписывай молча.

🔴 **Долг, который наступает именно здесь — файлы зеркала.** Сейчас зеркало несёт **ключ объекта**,
и это работает, пока файл лежит в общем хранилище. После переезда базы и файлов на машину преподавателя
ключ будет указывать **на его компьютер** — и обещание §20.3 «ученик откроет свою работу всегда»
сломается тихо: запись в зеркале есть, файл не открывается. **В Р5.2 зеркало обязано физически копировать
файлы работ ученика в хранилище точки встречи**, а не ссылаться на машину преподавателя.
Тест: выключили машину преподавателя → ученик открыл своё сочинение.

**Долг Р5.1:** аутентификация машины идёт аргументом (`hostHeartbeat`, `reportUplink`) — принято
как временное. В Р5.2, когда известна форма sidecar, свести к одному пути через `Authorization`.

### 🔴 Два адреса — решение ревьюера 14.08 (вопрос исполнителя из Р5.4)
Приложение обязано ходить в **две разные точки**, и это не недосмотр конфигурации, а суть
архитектуры: точка встречи должна отвечать, **когда машина преподавателя выключена**, — иначе
рушится и экран «преподавателя нет в сети» (§20.2), и зеркало ученика (§20.3).

**Правило маршрутизации — по смыслу операции, не по удобству:**

| Куда | Что туда идёт | Почему |
|---|---|---|
| **Точка встречи** (сервер) | связывание машины · presence и heartbeat · ссылка группы и вход по ней · зеркало ученика · расписание · чаты | обязано работать без машины преподавателя |
| **Sidecar** (127.0.0.1) | всё остальное: программа курса, доски, методички, работы, оценки, живой урок | живёт на машине и умирает вместе с ней |

Требования к реализации:
- **две переменные окружения**, а не одна: адрес точки встречи и адрес локального API;
- маршрутизация **явным списком операций** (split-link по имени операции), не эвристикой
  и не «если локальный не ответил — пойдём в сеть»: молчаливый переход туда-сюда
  однажды утащит на сервер то, чему там не место;
- **тест на список**: каждая операция из колонки «точка встречи» существует в её схеме,
  и ни одна операция локального API в этот список не попала.

**Приёмка:** `settings_desktop.py`, приложение стартует на чистой машине, весь пользовательский путь
работает без внешнего Postgres. Подписи установщика **нет** — решение владельца 14.08; на странице
скачивания обязано быть предупреждение про «неизвестного разработчика» (текст — в D6).

### Р5.3 — Настоящая сборка, затем рама приложения (лист D1)
**Сначала сборка, потом интерфейс.** ADR-001 закрыт по миграциям и по старту, но три числа
в нём отсутствуют, и получить их можно только настоящей сборкой Tauri с Rust-тулчейном:
**размер установщика · первый запуск после установки · PyInstaller против venv**.
Пока их нет, решение «десктоп» держится на измерениях лаборатории, а не на том, что скачает человек.

Вместе со сборкой (по одному и тому же поводу — мерить надо собранное, а не воображаемое):
`requirements-desktop.txt` без `boto3` · `redis` · `celery` (~30 МБ). ⚠️ Перед тем как выкинуть `boto3`,
проверить, чем десктоп грузит файлы работ в точку встречи: если через наш API — не нужен, если
напрямую в объектное хранилище — нужен. Не выкидывать по списку, выкинуть по факту.

Затем сама рама:
Заголовок окна с опознанием урока · состояние хоста · полоса состояния с переключателями окон
и раскладок класса · окно «Класс» (вдвоём / группа / ученик рядом, преподаватель виден всегда) ·
связь словами · сворачивание в трей без завершения урока · экран «нет сети» с фразой «данные в целости».

> **Демо-проекции — долг, а не отмена.** Запросы Р5.0/Р5.0-Б вышли без демо-проекций: набор полей
> определяется экраном, а экранов ещё нет — довод принят. Но правило PROMPT_13 §2.5 не отменяется:
> **Р5.4 и Р5.6 не принимаются без демо-проекций всех запросов, которые их экраны трогают.**
> Витрина `flamingo.plus` обязана работать после каждой фазы, а не «когда-нибудь».

### Р5.4 — Первый запуск (лист D2)
Пять шагов: связывание кодом · папка кабинета · **обязательная копия** · согласия · проверка камеры,
микрофона и канала · «готово». Плюс экран настроек, где живут числа связи.
Анализ внимания **по умолчанию выключен** (D2, §19).

### 🔴 Р5.4-Б — Дотянуть зеркало до полной базы знаний ученика. **Делается ПЕРВОЙ**
Уточнение владельца 14.08 (OWNER_SCOPE §20.5): у ученика хранится **вся его учёба**, а не только
написанное им. Зеркало Р5.0-Б несёт работы, оценки и саммари — этого мало. Добавить:

1. **Дневник** — занятия, посещаемость, оценки, прогресс, достижения (частично уже есть — свести в один вид).
2. **Доски и майндмапы его занятий** — то, что класс видел на экране.
3. **Методички и материалы, выданные ему или его классу — содержимым**, не именем и не ссылкой.
   Прежняя строка §20.4.1 про «имя и ссылку» **отменена владельцем**.
4. **Чаты** — предмета, с преподавателем, с одноклассниками.

Граница не меняется: **выдал классу → лежит у ученика; не выдал → своё**. Тест границы
(`test_an_unshared_material_never_appears_in_a_mirror`) остаётся и должен продолжать кусаться.

**Приёмка:** учётка преподавателя удалена → ученик открывает дневник, свои работы **с файлами**,
саммари, доски своих занятий и выданные методички. Невыданный черновик — не открывает.

### 🔴 Р5.5 — Кабинет одним файлом. **Идёт раньше Р5.6**
По итогам Р5.4: первый запуск **заставляет** преподавателя выбрать копию (§19.1), но **писателя
копии не существует** — `configure_backup` записывает вид копии, а файла не делает никто.
Единственное место, где мы принуждаем, пока не выполняет обещанного.

⚠️ **Поправка ревьюера к собственному доводу** (§20.5.3): «ноутбук уносит работы и оценки детей» —
неверно, когда зеркало полное. Учёба ребёнка живёт отдельно и переживает поломку. Копия кабинета
защищает **работу преподавателя** — программы, черновики, невыданные материалы. Отсюда и порядок:
сначала Р5.4-Б, потом Р5.5.

**Приёмка:** не «код написан», а «сняли копию → снесли кабинет → восстановили → всё на месте».

#### 🔴 Р5.5-Б — копия обязана покинуть машину
По итогам Р5.5: файл снимается и восстанавливается, но **кладётся в папку кабинета**, то есть
на тот же диск. Копия на том же диске — не копия: тот же пролитый кофе уносит и оригинал, и её.
На первом запуске мы обещаем «на внешний диск или в папку облака» (§19.1), и обещание пока не
выполняется.

Делает **оболочка**, не sidecar: выбранный преподавателем адрес живёт на машине и через GraphQL
не ходит — это верное решение исполнителя, его не менять. Нужна команда Tauri: выбрать место
однажды, дальше переносить туда файл после каждой копии, показывать в настройках дату и место
последней **удавшейся** копии. Место исчезло (диск отключён) — сказать словами, а не молча
писать в старую папку.

**Приёмка:** копия появилась вне папки кабинета; отключили диск — приложение сказало об этом.

### Р5.5 — Кабинет одним файлом
`common/portability.py` → рабочий экспорт и импорт: один файл, версия формата, клиентское шифрование,
восстановление на чистой машине. Расписание копии: раз в день и после каждого занятия.
Это же — фундамент будущей синхронизации (OWNER_SCOPE §18, Р5.2).

**Приёмка:** тест «выгрузили → снесли → восстановили → всё на месте», состав выгрузки сверен с `CABINET_TABLES`.

### Р5.5-В — три дыры честности (решения ревьюера 14.08 по итогам Р5.4-Б/Р5.5-Б)

Мелкие по коду, крупные по последствиям: в каждой продукт обещает то, чего не делает.

**1. Отдельный диалог для места копии — да, но без фильтра.**
«Где кабинет» и «куда возить копии» — разные вопросы, один диалог их путает. Фильтр
на съёмные тома **не ставить**: папка облака (iCloud, Яндекс.Диск) — законное место копии,
а фильтр её отрежет. Вместо запрета — **предупреждение словами**, если выбранное место
лежит внутри папки кабинета или на том же томе: «копия на том же диске не спасёт от поломки».
Предупредить и пустить — §2.2-бис.

**2. 🔴 Занятие закрывается само.** Сейчас дневник пишется на «Завершить»; преподаватель
закрыл ноутбук — записи за занятие нет, и посещаемость с оценками до ученика не доедут.
Точка встречи видит presence: **пропал heartbeat хоста на 10 минут при живом занятии — закрыть
занятие и написать дневник**, пометив его честно («занятие завершилось само»). Не выдумывать
длительность и присутствие — писать то, что известно на момент обрыва.

**3. Копию будит оболочка.** Celery отложен, планировщика нет — и не заводить. Расписание
§19.1 исполняет приложение: **при старте (если `backup_is_due`) и по окончании занятия**.
Оба момента уже есть в коде, таймер не нужен.

### Р5.6 — Приглашение и вход ученика (лист D3, веб)
Ссылка группы + код, режимы доступа (**по умолчанию «только ученики группы»**), список участников
с состояниями, экраны ученика: ожидание с проверкой камеры · «урок идёт, заходите» ·
**«преподавателя нет в сети»** · «занятие закончилось» · «ссылка заменена».
Домашняя работа при офлайне хоста пишется и **уходит сама**, когда хост появился.
Свои работы, оценки и саммари ученик открывает **всегда** — из зеркала Р5.0-Б; недоступна только
живая доска и методички текущего урока.

Лист утверждён, блокировок нет. Серверная часть готова в Р5.0.

### Р5.7 — 🖊 Планшет как доска (решение владельца 14.08, OWNER_SCOPE §21.2)
Занятие идёт с компьютера, планшет работает пером. **Это не второе устройство на уроке, а перо
для той же машины** — от этой формулировки зависит вся реализация.

- **Не участник:** нет места в классе, нет строки в списке участников, нет второго видео.
  С планшета идут **только штрихи** (десятки кбит/с) — исходящий канал преподавателя не растёт,
  доску классу по-прежнему раздаёт компьютер.
- **Подключение — тем же шестизначным кодом**, что в Р5.0 (связывание машины) и Р5.6 (вход по ссылке).
  Планшет открывает страницу в браузере, вводит код. **Третьего механизма не изобретать.**
- **Родного приложения нет.** Перо — через Pointer Events (`pointerType === 'pen'`);
  касание ладонью игнорируется, пока перо в работе.
- **Отвалился — урок не заметил:** доска продолжает работать с компьютера, повторное подключение
  по тому же коду без перезапуска занятия.
- **Не строить сейчас:** планшет как второй экран и перо в руках ученика. Заложить так,
  чтобы потом не переписывать.
- 🔴 **Планшет не пишет в базу.** Штрихи идут в тот же процесс, что ведёт урок, и он один
  распоряжается доской. Если реализация склоняется ко второму писателю в SQLite — это признак,
  что планшет опять посчитали устройством, а не пером; вернуться к §21.2 п.1, а не решать
  блокировками записи.

**Приёмка:** штрих на планшете виден классу; исходящий битрейт преподавателя **не изменился**
(замерить и показать числа); отключение планшета посреди урока не роняет доску;
в списке участников планшета нет.

---

## 4. ФОРМАТ ОТЧЁТА (каждая фаза)

1. **Что сделано** — по пунктам, с путями файлов.
2. **Числа гейтов до → после** (pytest / vitest / ruff / black / eslint / npm run build).
3. **Что из работающего могло пострадать и чем доказано, что цело.**
4. **Новые инварианты §2.2** — какие тесты их держат.
5. **Расхождения с листом атласа** — если лист физически нереализуем, это отдельный пункт с обоснованием.
6. **Что требует решения владельца** — списком, без домыслов.
7. **Дрейф SDL** — `docs/flamingo_schema.graphql` правится **руками**, `export_schema` только для сверки.

---

## 5. ЧТО ЖДЁТ ОТВЕТА ВЛАДЕЛЬЦА (не додумывать)

**Открытых вопросов нет** — три вопроса по D3 закрыты 14.08 (OWNER_SCOPE §20), блокировок у фаз нет.

Следующая развилка появится на листах **D5** (сбои на уроке: что делает ученик, если преподаватель
отвалился посреди занятия) и **D4** (данные и копия). До них — не додумывать и не забегать вперёд.

Правило прежнее: любое решение, которого нет в OWNER_SCOPE или в листе атласа, **не принимается
исполнителем самостоятельно** — оно выносится в отчёт отдельным пунктом.

---

## 6. ЧЕГО НЕ ДЕЛАТЬ

- Не хранить видео, аудио, дословную расшифровку — **включая локальную базу**.
- Не принимать пароль в приложении ни в каком виде.
- Не класть токен машины в файл конфигурации или localStorage.
- Не выбрасывать серверный контур: он остаётся запасным путём.
- Не покупать подписи установщиков — решение отложено.
- Не реализовывать D3 до ответов §5 и не «улучшать» утверждённые листы по своему вкусу.
- Не трогать `_archive/` как источник дизайна.
