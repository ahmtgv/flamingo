# Генеральный RnD · ночь 18.08.2026 (промпт 31)

Не проверка по списку. Ниже для каждого направления: **что делал · что увидел · что из
этого дефект**. «Ничего не нашёл» пишется только рядом с описанием того, что именно пробовал.

---

## §1 · Кабинет владельца переживает встречу со свежим сервером

**Что делал.** На машине нашёлся настоящий кабинет (`~/Library/Application Support/
plus.flamingo.desktop/cabinet`, заведён 17.08 в 10:28 сервером от 15.08). Работал на КОПИИ,
оригинал не трогал. Данных в нём не было — связывание шло с удалённым сервером, — поэтому
положил их сам, **по его собственной старой схеме** (прямым SQL, а не новым кодом: новый
код уже знает про столбцы, которых там нет). Положил преподавателя с согласием 152-ФЗ и
связанную машину с пройденным мастером, настроенной копией кабинета и включённой облачной
копией. Затем накатил свежий сервер.

**Что увидел.**

```
Applying accounts.0009_user_middle_name...                      OK
Applying accounts.0010_user_consent_attention_device_id_and_more OK
Applying homework.0004_homework_max_score...                     OK
Applying oversight.0002_..._accountstaterecord...                OK

человек: Адель Ахметгареев | отчество: «» | 152фз=1 | согласие на внимание=0
машина:  MacBook Адель | шаг мастера=5 | копия=folder | облако=1 | отозвана=нет
```

Миграций оказалось **четыре, а не семь**: часть из семи в кабинете уже стояла. Человек и
машина уцелели целиком — имя, согласие 152-ФЗ, шаг мастера, вид копии, флаг облака, не
отозвана. Новые столбцы добавились пустыми, что и правильно: отчества никто не вводил.
Новые таблицы надзора и максимум оценки у задания на месте.

**Дефектов нет.** Но сказано это теперь по результату, а не по наличию механизма.

**Подпись.** `codesign --verify --deep --strict --verbose=2`:

```
Flamingo.app: valid on disk
Flamingo.app: satisfies its Designated Requirement
код возврата: 0
```

Пересобранный сайдкар подписан вместе со всем остальным — вложенные бинарники проверку
`--deep` проходят.

---

## §2.1 · Урок на двоих — ЗАМЕРЕНО, ДВЕ НАХОДКИ

**Что делал.** Два независимых контекста браузера — два разных человека со своими сессиями,
а не две вкладки одного. Оба входят ФОРМОЙ (почта, пароль, кнопка), оба открывают доску
одного живого занятия. Преподаватель рисует пером мышью — движение, нажатие, протяжка,
отпускание. Обрыв настоящий: `setOffline(true)` рубит сеть контексту целиком.

**Что увидел.**

```
[живая подписка] +500мс:  у преподавателя 1, у ученика 0
[живая подписка] +1500мс: у преподавателя 1, у ученика 0
[живая подписка] +3000мс: у преподавателя 1, у ученика 0
[живая подписка] +5000мс: у преподавателя 1, у ученика 0
[обрыв]          преподаватель дорисовал до 4, у ученика 0
[возврат]        +1.5с: 4 · +3с: 4 · +5с: 4   — сошлись
```

**Что из этого дефект — два разных.**

**(1) Доска ученику вживую НЕ ПРИХОДИТ.** Преподаватель пишет — класс пять секунд видит
пустой холст. Единица у преподавателя не от подписки, а от его же локального рисования, то
есть не приходит НИКОМУ. Проверено на двух разных стендах: на тестовом контуре с перехватом
запросов и на dev-сервере, где сокет идёт туда же, куда запросы. Причина сужена, но не
изолирована: права ни при чём (ученик доску читать вправе — сервер отдаёт ему 4 элемента),
канальный слой на месте, адрес сокета верный. **Не починено.**

**(2) Досинхронизация не срабатывала на настоящем обрыве — ПОЧИНЕНО.** Я сделал её в промпте
28 и проверил СО СТОРОНЫ СЕРВЕРА: полный запрос действительно отдаёт всё. Чего не проверил —
дёргает ли клиент этот запрос. Замер показал: **не дёргал**, ноль запросов доски после
возврата сети. Все три моих крючка мимо: вкладка не прячется, фокус не теряется, `graphql-ws`
переподключается сам и `onComplete` не зовёт. Добавлен `online` — прямое событие браузера о
возврате сети. После правки: 1 запрос, картины сошлись.

⚠️ Вместе эти два означают: **до сегодняшней ночи ученик не видел доску преподавателя
никогда** — ни вживую, ни после обрыва.

### §2.1-б · Обрыв у ПРЕПОДАВАТЕЛЯ — третья находка

**Что делал.** То же двумя контекстами, но сеть рву у того, кто ведёт. Преподаватель рисует
штрих ДО обрыва, потом ещё один — уже без связи, не зная об этом. Затем связь возвращаю и
спрашиваю ученика начисто (перезагрузкой), доехало ли нарисованное.

**Что увидел.**

```
преподаватель офлайн, экран: «…идёт · Вывести на второй экран · Анализ внимания…»
нарисовал без связи, у себя видит: 1   (было 1 — то есть штрих не появился даже у него)
сказано ли ему про связь:              ни слова
связь вернулась, у преподавателя:      1
у ученика после всего:                 1
```

**Дефект.** Преподаватель рисует во время короткого провала связи — **штрих пропадает молча**.
Ни на экране, ни после возврата ему не говорят, что связи не было и что нарисованное не
сохранилось. Он продолжает вести урок, уверенный, что класс видит написанное. **Не починено.**

### §2.1-в · Две вкладки одного ученика

**Что делал.** Ученик открывает вторую вкладку того же занятия в той же сессии.

**Что увидел.** `первая вкладка: 0, вторая: 1` — свежезагруженная вкладка показывает штрих
преподавателя, а та, что была открыта всё время, не показывает ничего.

**Дефект.** Тот же, что находка 1, с другой стороны: живая подписка молчит, а начальная
загрузка работает. Ценно тем, что подтверждает механизм независимо: расходятся два окна
ОДНОГО человека с одной сессией — значит дело не в правах и не в том, кто смотрит.

---


## §2.2 · Слабый канал — граница названа

**Что делал.** WebKit полосу ограничивать не умеет (нет CDP), поэтому взял Chromium и
`Network.emulateNetworkConditions` — единственный движок, который умеет задать полосу и
задержку. Четыре ступени из жизни: 4G хороший (12 Мбит/40 мс), 3G городской (1.6 Мбит/150 мс),
3G плохой (400 Кбит/400 мс), край (128 Кбит/800 мс — школьный вайфай на тридцать человек).
Мерил ДВАЖДЫ, и это оказалось важно.

**Что увидел.**

Против dev-сервера (он отдаёт сотни отдельных модулей):

```
4G хороший   вход 7.2с   комната 2.3с
3G городской вход 50с    комната 8.2с
3G плохой    вход 199с   комната 20.5с
край         не дошёл
```

⚠️ Эти числа — про СТЕНД, а не про продукт, и я едва не записал их в отчёт как продуктовые.
Против СОБРАННОГО фронта (один файл 1.5 МБ + 190 КБ стилей — то, что получит преподаватель):

```
4G хороший   0.6с
3G городской 2.6с
3G плохой    9.7с
край         29с
```

**Дефект — вторая половина ворот.** Продукт открывается даже на 128 Кбит, за 29 секунд. Но
**на всех четырёх ступенях экран говорит одно и то же**: ни слова о канале, ни намёка, что
идёт медленно. Человек на плохом 3G десять секунд смотрит в пустоту и решает, что сломалось.
Граница пригодности: до 400 Кбит урок начать можно (10 с ожидания), на 128 Кбит — тридцать
секунд молчания, и это уже не «медленно», а «не работает». **Не починено.**

## §2.4 · Системная строка macOS — впервые увидена глазами

**Что делал.** Собрал приложение, запустил, снял экран, вырезал верх окна.

**Что увидел.** Светофор macOS стоит **внутри нашей строки заголовка** — ровно как просит
лист D1. Второй системной полосы нет: правка `titleBarStyle: Overlay` из промпта 26 работает,
и это подтверждено глазами, а не выведено из конфигурации.

**Дефект.** Под строкой рамы стояла **вторая полоса с тем же знаком бренда** — стартовая
рисует свою шапку внутри приложения. В промпте 26 я спрятал такую шапку в КОМНАТЕ и не
тронул стартовую. Убрал знак (навигацию оставил: чат, источники, тема, выход, учётная запись
— их прятать нельзя, а бренд рама уже несёт). **Починено.**

⚠️ Проверить починку глазами в этом заходе не вышло: пересобранное приложение открылось на
мастере первого запуска (шаг 2 из 5), а не на стартовой. Правка подтверждена сборкой и
кодом, но не снимком. Сказано, а не умолчано.

**Не проверено в §2.4:** другой пользователь macOS с чистым домашним каталогом, другой язык
системы, 13 дюймов, тёмная тема.

## §2.3 · Долгая работа — НЕ НАЧАТО
## §2.6 · Пустой продукт, уход человека, две группы — НЕ НАЧАТО

Ни одного наблюдения. Ни строки «проверено».

---

# ЗАХОД 18.08, ВТОРАЯ ПОЛОВИНА — ПРОМПТ 34

## §1 · Восемь подписок, проверенные поведением — 8 из 8

Прибор: `frontend/scripts/subs-probe.mjs`. Открывает настоящий веб-сокет
(`graphql-transport-ws`) **как второй человек**, потом вызывает событие от первого и печатает
**кадр, который пришёл второму**. Не «резолвер есть» и не «схема называет» — этими двумя
доводами список живых подписок держался, пока 16.08 все восемь не оказались мёртвыми разом.

```
✅ chatMessageReceived      {"chatMessageReceived":{"id":"…","text":"здра…
✅ projectorFocusChanged    {"projectorFocusChanged":{"sessionId":"…"}}
✅ attentionUpdates         {"attentionUpdates":{"studentId":"…","avgAtte…
✅ signals                  {"signals":{"kind":"OFFER"}}
✅ channelMessageReceived   {"channelMessageReceived":{"id":"…","text":"п…
✅ hostPresenceChanged      {"hostPresenceChanged":{"slug":"…","online":true}}
✅ wordShown                {"wordShown":{"sessionId":"…"}}
```

Восьмая — `boardChanged` — сторожится обычным прогоном, `e2e/live.spec.ts`.

⚠️ Три из семи потребовали **класса**, а не курса-программы: канал предмета, точка встречи
группы и словарь урока живут на группе. Через API класс не завести — `createInstitution`
требует сотрудника платформы (ровно то, что нашёл ролевой аудит промпта 27), поэтому прибор
сеет класс напрямую и говорит об этом вслух.

## §1.1 · Сторож живого слоя — в обычном прогоне

`e2e/live.spec.ts`: два независимых контекста браузера, вход формой у обоих, доска одного
занятия. Преподаватель ведёт штрих — **у ученика, который ничего не делает, становится на
штрих больше**. Не помечен `skip`, не зависит от флага.

**Проверено обратным ходом.** Вернул `getattr(context, "connection_params")` (дефект 16.08) —
сценарий покраснел; убрал — позеленел. Сторож, который не краснеет на сломанном, не сторож.

🔴 **Чтобы это стало возможным, пришлось перехватывать СОКЕТ.** В сборку вшит боевой адрес и
запроса, и сокета; запрос перехватчик уводил на контур, а подписка уходила на
`wss://api.flamingo.plus`, где урока из прогона нет. Поэтому единственный сценарий на живую
подписку жил отдельно, против dev-сервера, под флагом — то есть в обычном прогоне не
участвовал. `routeWebSocket` умеет перехватить, но не умеет переподключить на другой адрес,
поэтому в `e2e/apiProxy.ts` появился мост: сокет до контура в Node и переливание кадров в обе
стороны. Кадры до открытия моста копятся — `graphql-ws` шлёт `connection_init` первым тактом.

Прогон: **16 из 16** (было 15).

## §1.2 · CLAUDE.md — «живая» теперь значит одно

Строка «Live today (8)» держалась на том, что резолвер существует и SDL его называет. Оба
довода 16.08 оказались ничего не стоящими. Теперь в CLAUDE.md записано: **«живая» = второй
человек получил кадр**, список не выводить из SDL и из `schema.as_str()`.

## §2.1 · Штрих во время провала связи — ПОЧИНЕНО

**Было** (замер 18.08): преподаватель рисует, пока связь на секунду пропала. Штрих не
появляется даже у него, не уходит никому, не возвращается после связи — и **ни слова на
экране**. Он ведёт урок дальше, уверенный, что класс видит написанное.

**Причина — одна строка `catch`.** Любой отказ считался отказом ДОСКИ («закрыли посреди
правки») и лечился перезапросом с сервера, то есть затиранием только что нарисованного.
Отказ доски и «запрос не дошёл» — разные события, и продукт уже умеет их различать
(`shared/lib/requestFailure.ts`), просто здесь не различал.

**Стало** (тот же прибор, `rnd.spec.ts` → «§34 §2.1»):

```
нарисовано без связи, видит у себя: +1
сказано ли ему про связь: «Связи нет. Класс вас сейчас не видит — написанное на доске
                           сохранится и уйдёт, как только связь вернётся»
                          «Связи нет: 1 фигура ещё не ушла классу»
после возврата: у ученика 2 (было 1)
```

⚠️ Порядок при возврате связи — часть починки: сначала досылаем своё, потом забираем чужое.
`resync` заменяет холст серверной картиной, и всё написанное без связи пропало бы ровно в тот
момент, когда связь вернулась.

## §2.2 · Одна строка про связь на весь продукт

`shared/lib/connection/` — вывод; `shared/ui/ConnectionLine` — показ. Одна ссылка Apollo
(`connectionLink`) наблюдает **каждый** запрос продукта, поэтому механизм общий без единой
строчки на экранах.

Три правила владельца, каждое видно в коде:

* **тихо на обычных экранах, заметно на уроке** — громкость выбирает экран, не механизм;
  рама показывает тихую строку, урок забирает разговор себе (`claimLoudConnection`);
* **что происходит, а не «что-то пошло не так»** — слово о канале и что именно с ним;
* **не мигающая плашка на каждый запрос** — один не дошедший запрос связь не роняет
  (`LOST_AFTER = 2`), ухудшение показывается сразу, улучшение придерживается 3 секунды.
  Тест «дребезг связи не даёт мигания» гоняет канал туда-сюда десять раз за секунду.

Браузеру верим только в одну сторону: `navigator.onLine === false` — точно нет сети, `true` не
значит ничего (школьный вайфай отвечает `true`, пока не доходит никуда).

## §3.1 · Долгий урок — ПРИБОР ВРАЛ, ЧИСЛА ПЕРЕМЕРЕНЫ

Первый заход показал «доска встала на пятнадцатой минуте» — ровно срок жизни токена
(`ACCESS_TOKEN_LIFETIME_MIN = 15`). Совпадение выглядело исчерпывающим объяснением.

**Оно было ложным.** Поднял отдельный стенд с минутным токеном и перемерил:

```
запросы после истечения: PutBoardElement → «Authentication required» → Refresh → PutBoardElement ✅
штрихов: 3 у обоих
```

То есть **истечение сессии посреди урока продукт переживает правильно**: отказ, молчаливое
обновление, повтор. Дефекта нет.

А «встала доска» оказалась моим окном: 1280×720 по умолчанию, холст начинается на y≈525 и
уходит на 360 вниз — его нижние две трети **за окном**. Мышь туда не доносит, штрих не
рождается. Числа штрихов первого захода недействительны; числа памяти — действительны.

Память за 31 минуту урока с рисованием: 38 МБ ровно до 19-й минуты, ступенька до 61/58 МБ на
20-й, дальше ровно. Утечки не видно. Ошибок в консоли — ноль.

## §3.3 · Чужая машина — 13 дюймов и тёмная тема

Сняты экраны в 1440×900 и в окне приложения (1280×860), светлая и тёмная. Тёмная тема на
входе и в хабе источников: контраст в порядке, токены отрабатывают, знак бренда один.

**Не проверено:** другой пользователь macOS с чистым домашним каталогом, другой язык системы.

## §5 · Четыре подписки жили на сервере и не открывались продуктом

Замер §1 доказал, что сервер публикует все восемь. Отдельный вопрос — **спрашивает ли кто-то**:

| подписка | документ во фронте | вызов хука |
|---|---|---|
| `attentionUpdates` | есть | есть |
| `boardChanged` | есть | есть |
| `projectorFocusChanged` | есть | есть |
| `wordShown` | есть | есть |
| `channelMessageReceived` | есть | **НЕТ** |
| `hostPresenceChanged` | **НЕТ** | **НЕТ** |
| `chatMessageReceived` | **НЕТ** | **НЕТ** |
| `signals` | **НЕТ** | **НЕТ** |

**Чат урока.** В коде стояло `pollInterval: 15_000` с комментарием «the socket carries the
live case» — а сокета не было. Сообщение доходило до собеседника за пятнадцать секунд, и
комментарий утверждал обратное. Подключено; замер: **61 мс** (опрос дал бы до 30 000).

**Ученик у двери.** `hostPresenceChanged` не был описан ни одним документом. Появление
преподавателя ученик узнавал опросом. Подключено.

Долг сирот: **44 → 42**.

## §5 · Дверь в повторение

Строка «повторение» на стартовой появлялась ТОЛЬКО когда что-то созрело. У ребёнка, набравшего
слова на уроке и открывшего продукт до срока, ссылки на `/repetition` не было нигде — экран
построен, маршрут заведён, попасть нельзя. Теперь дверь есть, пока у ученика есть слова;
ноль созревших — отдельная фраза, а не «0 карточек ждут».

## §5 · `/local-files/` — адрес, который продукт отдавал, и маршрут, которого не было

`common/storage.py` в десктопном профиле возвращает клиенту `/local-files/<key>` — и на
загрузку, и на чтение. В `config/urls.py` стоял ОДИН маршрут: `graphql/`. То есть внутри
приложения загрузка файла уходила в 404, а всё уже загруженное не открывалось: материал урока,
работа ученика, аватар.

Комментарий в `storage.py` при этом утверждал: «the sidecar serves its own loopback upload
route». Не служил. Третий раз подряд один и тот же механизм: половина написана, вторая
описана словами в комментарии, и никто не спросил, есть ли она.

Маршрут построен с жёсткими границами: только локальное хранилище (на боевом — 404, вторая
дверь к объектам мимо подписанной ссылки недопустима), только петля (не полагаемся на
`ALLOWED_HOSTS`: настройку меняют ради удобства, а это про диск), ключ не выходит за корень.
Тест берёт адрес **у самого продукта**, а не повторяет догадку о нём — именно там и была дыра.
Доказано отказом: без маршрута краснеют три проверки из пяти.

## §4 · Без отметок — дошкольники и первый класс

Основание внешнее: ФГОС НОО (приказ Минобрнауки РФ № 373 от 06.10.2009, ред. приказа
Минпросвещения № 286 от 31.05.2021) и ФЗ-273.

`GradingScale.MARKLESS` существовал в перечислении с промпта 28 и **не значил ничего** — ни
одна строка кода его не проверяла. Слово в перечислении — не правило.

Теперь правило одно на продукт (`common/marking.py`) и стоит в сервисе, а не на экране:
экран можно обойти, запрос — нельзя. Год обучения читается из свободного поля профиля
(«1», «1 класс», `grade_1`, «дошкольник») одним разбором: свой разбор в каждом месте разошёлся
бы на первом же «1 класс». Неизвестный год — НЕ безотметочный: иначе продукт придумал бы
ограничение вместо закона.

**Вторая половина требования — значки.** Звёздочки, баллы, полоски, «уровни», рейтинги — это
та же отметка другим шрифтом. Сторож `frontend/src/shared/ui/markless.test.ts` спрашивает не
«нет ли где слова балл», а одно: **экран, показывающий ученику оценку в любом виде, обязан
спросить `markless`**. Прибор проверяет себя двумя тестами — сканеры здесь уже дважды врали.

Сторож покраснел на живом коде и назвал три экрана: старый кабинет ученика (баллы), экран
домашних заданий ученика (оценка), панель заданий кабинета предмета (оценка по шкале курса).
Все три исправлены; шкала кабинета предмета теперь считается для ПАРЫ (ученик, курс).

**Сторож нашёл и мою собственную ошибку.** Первая версия проверки смотрела на
`submission.student.student_profile`, а `Submission.student` — это УЖЕ профиль ученика.
`getattr(..., None)` возвращал None, и отметка первокласснику проходила молча. Тест покраснел
на «DID NOT RAISE» — без него правило существовало бы только на бумаге.

Проверено глазами: у работы первоклассницы поля отметки нет вовсе, на его месте «Без отметки —
первый класс», а поле рядом называется «Что получилось, что потренировать».

## §3.1 · Сорок минут урока — числа

Два браузера, оба в комнате, преподаватель рисует три штриха в минуту, снимок каждую минуту.

```
минута  преп.МБ  преп.узлов   учен.МБ  учен.узлов   штрихов   ошибок
  0       40        168         38        150         2/2        0
 10       40        218         38        200        27/27       0
 20       33        278         33        260        57/57       0
 30       33        330         33        312        83/83       0
 40       36        384         38        366       110/110      0
```

**Что видно.** Память не растёт — 40 → 33 → 36 МБ: сборщик мусора работает, утечки нет. Узлы
растут ровно на нарисованное (каждый штрих — свои узлы), а не сами по себе. **Картины не
разошлись ни разу за сорок минут: 110 против 110.** Ошибок в консоли ноль. Урок на сороковой
минуте жив, экран внимания на месте, надпись о приватности на месте.

⚠️ Проверено на dev-сервере с двумя вкладками одной машины. Ни LiveKit-видео, ни настоящих
двух устройств тут нет; это про накопление в веб-части, а не про нагрузку на канал.

## §3.2 · Хвосты урока на двоих

```
исходно у ученика: 1
пока вкладка в фоне: у ученика 3 (у преподавателя 3)   — доска доходит и в фон
вернулся на вкладку: сошлись на 3
закрыл вкладку и открыл заново: 5 против 5             — видит всё, что рисовали без него
```

Все три хвоста чистые. Это и подпись под починкой промпта 31: досинхронизация по `online` и
по возврату вкладки работает не только в сценарии, ради которого писалась.

## §3.4 · Слабый канал по ступеням — и граница самого механизма

Смотрел не на загрузку страницы (это про стенд), а на **запросы продукта** под CDP-ограничением:

```
4G хороший (12 Мбит)    запросов 14, самый долгий   55 мс   про связь: молчит
3G городской (1.6)      запросов 14, самый долгий  265 мс   про связь: молчит
3G плохой (400 Кбит)    запросов 15, самый долгий  779 мс   про связь: молчит
край (128 Кбит)         запросов 14, самый долгий 1885 мс   про связь: молчит
```

**Вывод важнее чисел: тонкий канал по маленьким запросам почти не виден.** Больно от него
загрузке страницы (1.5 МБ) и видео, а GraphQL-запрос отвечает быстро даже на 128 Кбит.
Первый порог (2500 мс) не срабатывал ни на одной ступени. Поставлен 1500 мс — полторы секунды
на маленький запрос человек чувствует, а на 400 Кбит продукт молчит, и правильно: журнал
провёл границу пригодности ровно там.

Сказано вслух в коде: **этот механизм ловит потерю связи и медленный сервер, а «канал тонкий»
им не ловится** — подгонять порог до срабатывания на 128 Кбит нельзя, он начнёт срабатывать на
здоровой сети. Тонкий канал во время урока меряет замер канала машины (`uplinkProbe`, лист D1);
словарь у двух приборов общий намеренно.

⚠️ **Три варианта этого замера подряд не мерили ничего, и каждый выглядел убедительно:**
перезагрузка страницы сбрасывала наблюдения (они живут в памяти вкладки); ручной `fetch` шёл
мимо ссылки Apollo; переходы по продукту били в кэш Apollo и запросов не поднимали. В сценарий
встроен счётчик запросов продукта — без него «продукт молчит на всех ступенях» было бы
выводом про прибор, а не про продукт. Это четвёртый такой случай за заход.

## §5 · Щипок трекпадом не работал никогда — НАЙДЕНО И ПОЧИНЕНО

Наряд просит проверить щипок **рукой**. Рукой я не могу; браузер шлёт то же событие, что шлёт
трекпад macOS, — `wheel` с `ctrlKey`. **Это эмуляция, и называть её надо так.**

Замер показал:

```
[ПРИБОР] события колеса, дошедшие до страницы: ["deltaY=-240 ctrl=true"]
[ЭМУЛЯЦИЯ щипка] штрих на экране: было 40px, стало 40px
```

Событие дошло — масштаб не изменился. Значит дефект в продукте, а не в эмуляторе (прибор
проверен первым, как и положено).

**Причина.** Слушатель вешался эффектом с пустым списком зависимостей. Пока доска грузится,
компонент отдаёт «…», и холста в разметке нет: эффект отрабатывает единственный раз, видит
`surface.current === null`, выходит — и когда холст появляется, вешать слушатель уже некому.
Щипок на доске не работал **ни разу с того дня, как его написали** (промпт 28 §2).

Тот же механизм, что и во всём заходе: код, который умеет ответить, написан и покрыт
комментарием; доходит ли до него вопрос — не спрашивал никто.

После правки: 40px → 89px. **Остаётся за человеком:** удобство жеста под пальцами — эмуляция
о нём ничего не говорит.

## Сборка: подписана, НЕ нотаризована — и почему

Образ пересобран после всех правок; `build-all.sh --check-only` подтверждает, что серверная
половина внутри свежая. Подпись Developer ID на месте:

```
codesign --verify --deep --strict   → valid on disk · satisfies its Designated Requirement
spctl -a -t install                 → rejected · source=Unnotarized Developer ID
```

🔴 **Нотаризацию пройти не удалось: профиль `flamingo-notary` пропал из связки посреди захода.**
Час назад он был на месте — этим же профилем в этом же заходе нотаризованы и приложение, и
образ (`status: Accepted`, талон прикреплён). К концу захода:

```
xcrun notarytool history --keychain-profile flamingo-notary
Error: No Keychain password item found for profile: flamingo-notary
```

Восстановить его я не могу и не должен: для этого нужен ключ App Store Connect (`.p8`),
`KEY_ID` и `ISSUER_ID` — секреты владельца, которым место в связке, а не в моих руках
(правило промпта 23). **Владельцу — одна команда у себя**, после чего образ нотаризуется:

```
xcrun notarytool store-credentials "flamingo-notary" \
    --key /путь/AuthKey_XXXX.p8 --key-id <KEY_ID> --issuer <ISSUER_ID>
bash desktop/sign-and-notarize.sh
```

⚠️ Заодно исправлено в самом скрипте: «профиля нет» и «не смог спросить Apple» были одной
веткой, и при отвалившейся сети скрипт советовал бы заводить профиль, который на месте.
Теперь это две разные новости.

---

# ЗАХОД 18.08, ТРЕТЬЯ ЧАСТЬ — ПРОМПТ 35

## §1.1 · Три источника про видео — как есть на самом деле

| источник | что говорит | правда |
|---|---|---|
| `CLAUDE.md` | «LiveKit (self-hosted), API only issues room tokens» | **self-hosted нет**: адрес в сборке — облако |
| `infra/prod/docker-compose.prod.yml` | «livekit — серверу здесь делать нечего», ретранслятор coturn, «не нужен иностранный посредник в цепочке детского видео» | это ПЛАН другого пути, у которого **нет клиента** |
| `frontend/.env` | `VITE_LIVEKIT_URL=wss://flamingo-atvyww1r.livekit.cloud` | **это и есть рабочий путь** |

**Что работает сегодня — путь А, LiveKit Cloud.** Замер (`scripts/two-people-video.mjs`)
показал живьём, куда идёт видео:

```
connected to Livekit Server edition: 1, version: 1.13.5, protocol: 17,
region: Germany 2, nodeId: NM_OFRANKFURT1B_SKnf83StUBrn
```

То есть **медиа двух российских участников идёт через Франкфурт**. Это ровно то, против чего
записан довод в боевом compose, — и это же единственное, что сейчас работает.

**Путь Б — свой ретранслятор — построен наполовину.** На сервере есть `apps/signalling`
(`signals`, `sendSignal`), `common/turn.py` (`turnCredentials`), в боевом окружении настроен
coturn (`TURN_SECRET`, `TURN_URLS`, `TURN_EXTERNAL_IP`). **Во фронте нет ни одной строки**:
ни документа на `signals`, ни вызова `turnCredentials`, `RTCPeerConnection` встречается
единственный раз — в замере канала, не в уроке. То есть **coturn в бою сегодня не обслуживает
ничего**, а `signals` — сирота, ожившая 18.08 и никем не открываемая.

⚠️ Ещё одна асимметрия: `LIVEKIT_API_KEY` и `LIVEKIT_API_SECRET` есть в `backend/.env`
разработчика и **отсутствуют в `infra/prod/.env.production.example`**. Значит на боевом
сервере токен подпишется запасным путём (`api_key="devkey"`, секрет — `SECRET_KEY` Django), и
облако его не примет. **Видео на боевом не заработает, пока ключи не заданы** — а отказ, до
сегодняшней правки, был бы молчаливым.

**Выбор пути — решение владельца** (§1.1 наряда). Цена каждого:

* **остаться на LiveKit Cloud** — работает сегодня, ничего строить не надо; цена: медиа детей
  идёт через Германию, зависимость от иностранного поставщика, плата за трафик;
* **достроить свой путь** — медиа не покидает страну (coturn уже стоит и оплачен); цена:
  клиентская половина WebRTC целиком, ограничение на размер класса (P2P ≈ до 4–6 человек без
  SFU), и это отдельная фаза, не хвост.

## §1.2 · Двое живых людей — ВИДЯТ И СЛЫШАТ ДРУГ ДРУГА

Прибор `frontend/scripts/two-people-video.mjs`: два браузера, фиктивные камеры
(`--use-fake-device-for-media-stream` даёт настоящий медиапоток без железа), обёртка над
`RTCPeerConnection` ставится в страницу **до** загрузки приложения — продукт для замера не
тронут.

```
[преподаватель] connection state changed: connecting -> connected  room RM_gTzS77Fkj236
[ученик]        connection state changed: connecting -> connected  room RM_gTzS77Fkj236
[преподаватель] webrtc: удалённых дорожек 8, ice checking → connected
[ученик]        webrtc: удалённых дорожек 6, ice checking → connected

[преподаватель] video: «Аня К. · нет данных» — поток есть, video, 640x480, играет
[ученик]        video: «Ирина П»            — поток есть, video, 640x480, играет
[оба]           audio: 1 элемент, дорожка audio:live, играет, громкость 1, не заглушен
```

**Ворота пройдены:** у обоих напечатано состояние соединения, и у обоих на экране чужая
дорожка. Не «токен выдан» — картинка и звук дошли до второго.

⚠️ Чего прибор не проверяет: слышно ли и видно ли **глазами**. Фиктивная камера отдаёт
синтетическое изображение, звук никто не слушает. Это остаётся человеку.

## §1.3 · Ставки ревьюера — три из четырёх подтвердились

* **`signals` был мёртв до 18.08** — подтверждено, но видео на нём не стоит: оно на LiveKit.
  Ставка верна по факту и не относится к работающему пути.
* **Адрес медиасервера в сборке** — подтверждено частично: адрес есть и верен, но `.env`
  подхватывается Vite во **всех** режимах, включая десктоп, где `.env.desktop` его не
  переопределяет. Отдельно: на боевом нет ключей, и там путь не заработает.
* **Согласие на камеру и микрофон** — путь пройден целиком: отказ в доступе даёт «Нет доступа
  к камере», продукт не падает и не молчит.
* **coturn** — подтверждено: учётные данные выдаются (`turnCredentials` отвечает), и он
  **не нужен** работающему пути. Сегодня он не обслуживает ничего.

## §1.4 · Молчащий чёрный прямоугольник — БЫЛ, ПОЧИНЕНО

Три отказа по отдельности, каждый измерен:

```
а) камеру не разрешили        → «Нет доступа к камере.»                        сказано
б) медиасервер не отвечает    → «Ваша камера в эфире»                          ВРАЛО
в) всё в порядке              → «Ваша камера в эфире»
```

Экран при (б) был **неотличим от исправного**. Две причины: подпись говорила про КАМЕРУ, а не
про ЭФИР; и `room.connect()` отклоняется поздно — к тому моменту эффект пересобирается и отказ
проглатывается, состояние навсегда `connecting`.

После правки: «Подключаем эфир…», а через 20 секунд — «Сервер видео не отвечает. Урок
продолжается — доска, чат и задания работают.»

⚠️ Прибор пришлось уличить дважды: `page.route` **не ловит веб-сокет**, и первый
«заблокированный» медиасервер не блокировался вовсе.

## §4 · `sessionStatusChanged` — построена, 9-я живая подписка

Была объявлена в SDL с самого начала: ни резолвера, ни публикации, три месяца строка
контракта. На живом уроке это значило: преподаватель нажимает «Завершить занятие», у него
меняется всё, а класс сидит в комнате, которая уже не идёт — камера горит, доска на месте,
ничего не сказано.

Построены публикация (`_announce_status` на начале и конце), резолвер с проверкой права на
чтение и подписка в комнате ученика. Замер прибором: **живых 8 из 8** плюс эта — вторая
сторона получила `{"sessionStatusChanged":{"id":"…","endAt":"20…"}}`.

## §4 · 42 сироты — разобраны по существу

| группа | сколько | что это |
|---|---|---|
| **забытая проводка** — сервис есть, клиента нет | 22 | `markLessonViewed` `setProgress` `setAttendance` `unenroll` `updateHomework` `archiveCourse` `deleteCourse` `updateMyName` `verifyEmail` `respondGuardianship` `myWords` `mySkillMastery` `boardSnapshots` `addSummaryItem` `setSummaryIntro` `countLiveAsClasswork` `openDirectChannel` `openStaffChannel` `openSubjectChannel` `meetingPointByCode` `myMirror` `mirroredFileUrl` |
| **задел под фазу, которой нет** | 10 | `attentionAnalytics` `sessionAttention` `recommendations` `dismissRecommendation` `backupUbp` `ubpBackup` `deleteUbpBackup` `recordCabinetBackup` `chatReports` `resolveChatReport` |
| **второй путь видео** — построен на сервере, клиента нет | 4 | `signals` `sendSignal` `turnCredentials` `uplinkProbe` |
| **учреждение и надзор** — владелец сказал не строить | 4 | `createInstitution` `institution` `group` `updateGroup` |
| **прочее** | 2 | `chatMessageReceived` (чат внутри саммари) · `setProjectorFocus` |

🔴 Две из них стоят рядом с живым уроком и заслуживают отдельной строки:

* **`setProjectorFocus`** — документ во фронте есть, вызова нет ни одного. То есть второй экран
  подписан на фокус, который **никто не может послать**: преподаватель включает проектор и не
  может навести его ни на кого. Ровно та же половинчатость, что и во всём остальном заходе.
* **`myMirror`/`mirroredFileUrl`** — зеркало ученика пишется на сервере и не читается ни одним
  экраном: работа копируется, показать её ребёнку нечем.

⚠️ Число сирот **не уменьшилось** (42 → 42): `sessionStatusChanged` не была сиротой — её не
существовало вовсе, так что подключение новой подписки долг не трогает.
