# Что в архиве — заход 20–21 августа

Файлы лежат плоско: листы открываются двойным щелчком, `support.js` и `tokens.css` рядом
и подхватываются сами. Прогон приборов — `audit-run.html`, он ищет листы в той же папке.

## Новые листы

| Файл | Что внутри |
|---|---|
| `Создание курса и занятия.dc.html` | курс (название, предмет, уровень, ритм, цена, обложка, черновик/публикация) и занятие (дата, время, длительность, тема, материалы, пояса учеников). Пять состояний у каждого экрана |
| `Телефон.dc.html` | пять мобильных листов 390 × 844: расписание (день и неделя), задания со сдачей, «Моя учёба», запуск занятия, чат |
| `Первая страница.dc.html` | витрина на 1280 / 1512 и вертикальный лист 390; ссылки ведут в собранные листы |
| `Источники мира.dc.html` | 36 источников, право словами, «что будет, если замолчит», частичный отказ по опросу |

## Правленые старые листы

| Файл | Что изменено |
|---|---|
| `Кабинет и учёба.dc.html` | календарь: 20 августа 2026 — четверг, неделя 17–23 (было 18–24 и «ср 20»). Обе роли и экран расписания |
| `Комната урока.dc.html` | пульт доски жил на `opacity: .3` и давал 1,53 при пороге 4,5 — прозрачность снята |

## Приборы и токены

- `audit.js` — семнадцатый прибор `mobileScroll`: на кадре с `data-mobile` прокрутка листа
  разрешена, но вложенных нет, горизонтальная — только у полосы с `data-strip-ok`.
- `audit-run.html` — в прогон добавлены четыре новых листа и шесть источников проектора.
- `tokens.css` — добавлена ступень `--fl-leaf-800` (#3d6412): зелёный ТЕКСТ детского режима
  давал 3,86 на приглушённой поверхности. Заливка осталась на 700.
- `support.js` — среда исполнения листов, не менялся; лежит здесь, чтобы листы открывались.

## Документы

| Файл | Что дописано |
|---|---|
| `ПРАВИЛА.md` | 4.3а размер шрифта токеном, а не числом · 4.7 прозрачность не иерархия · 4.8 третичный цвет не на приглушённой поверхности · 4.9 одно имя экрана во всех местах · 4.10 индикатор состояния — графика с порогом 3,0 · 8.6а прокрутка на телефоне · 8.10 право четырьмя формулировками · раздел 9 «Телефон» · раздел 10 «Создание» |
| `ПРИЁМКА.md` | семнадцать приборов, мобильный кадр в списке видов, индикаторы состояния в списке «смотрится глазами» |
| `ОТЧЁТ-САМОПРОВЕРКИ.md` | разделы 12–15: мобильные листы, находки ревьюера (10 px в полке вкладок, горизонтальная прокрутка в kids), создание курса и занятия, переключатель повтора и цена собственной правки |
| `ПЕРЕДАЧА.md` | состав файлов, правило прокрутки для телефона, что осталось не сделано |
| `ТОКЕНЫ-ИЗМЕНЕНИЯ.md` | `--fl-leaf-800` с замерами |

## Что осталось открытым

- Настольные листы держат служебные моно-подписи 11–12 px. Прецедент восьми листов;
  закрывать разом по пулу с полным перезамером, а не по одному экрану.
- Родитель — роли есть в брифе, экрана нет.
- Правка опубликованного курса, отмена и перенос занятия с уведомлением 14 ученикам.
- Прогон транзиций есть только у «Комнаты урока»; остальные листы снимаются в покое.
- `sc-for` в служебной полосе «Вокруг урока» не отрисовался, обойдено явными кнопками —
  причина не найдена.
