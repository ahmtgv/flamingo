# Вопросы дизайна — Flamingo, огранка очереди 1

Собираю по ходу работы. Ответ владельца пишу прямо под вопросом.

## 1. Первая страница

1.1 **Верхнее меню.** HUB с этого экрана убран и уходит в верхнее меню. Меню на снимках очереди 1 нет ни на одном экране — оно уже существует в коде или его предстоит нарисовать? Если нарисовать, это новый блок, а скелет заморожен: нужно ваше слово и перечень пунктов меню.
— ответ:

1.2 **Частичный отказ.** У экрана один источник (сервер комнат), а правило 6.5б говорит: один источник — отдельного состояния не требуется. Я взял за частичный отказ отказ камеры и микрофона: комната поднимается, лицо и голос нет. Годится такая трактовка или частичный отказ здесь не нужен вовсе?
— ответ:

1.3 **Строка сообщения.** В продукте на первой странице её нет: сообщения появляются и исчезают, отчего макет прыгает. На листе строка стоит всегда, в три строки высотой. Это добавление блока — подтвердите.
— ответ:

1.4 **Зелёный «лист» вместо «мха».** Вы выбрали лист (#619b21, заливка #4a7a16). В `tokens.css` лист описан как ДЕТСКАЯ семья, мох — как взрослая, и утверждённый кабинет преподавателя нарисован на мхе. Два пути: (а) `--color-go-*` в корне переводятся на лист, кабинет перекрашивается вместе со всеми; (б) лист остаётся только на экранах входа, и продукт живёт с двумя зелёными. Первый путь честнее, но затрагивает утверждённый образец.
— ответ:

1.5 **Опечатка на снимке.** Метка поля набрана как `КАК ВАС ЗОВУТ` с разрядкой, а в подсказке рядом «Под этим именем вас увидит класс» — двойное «имя» в двух строках подряд. Оставляю как есть (тексты не переписываю), но отмечаю.
— ответ:

## 2. Вход и регистрация

2.1 **Выбор роли.** Две плитки «Веду уроки» / «Учусь» сейчас читаются как два прямоугольника. Огранка: выбранная берёт сильную рамку и нейтральную заливку (ПРАВИЛА 5.7). Подтверждаете, что выбор роли остаётся нейтральным, без зелёного?
— ответ:

2.2 **Восстановление пароля.** Третий режим («Забыли пароль») на снимках не показан. Тексты в нём — какие в продукте? Мне нужны заголовок, строка-объяснение и подпись кнопки словами, чтобы не сочинять.
— ответ:

## 3. Flamingo HUB и витрина

3.1 **Снимка витрины фактически нет:** `5-витрина.png` совпадает с `4-хаб.png`, рамки источника на нём не видно. Перезаснимите или опишите словами: размер рамки, что в её шапке, где «Закрыть», где честная строка про запрет показа.
— ответ:

3.2 **Правый рельс** пуст на две трети высоты в состоянии «ничего не выбрано». Сжать рельс до высоты текста или оставить во всю высоту с объяснением? Это оформление, но выбор виден глазом.
— ответ:

3.3 **36 карточек одним ковром.** Развилка оформления: (а) карточки остаются, но группируются по видам с заголовками рядов; (б) плотный список строками — все 36 видно без прокрутки. Покажу оба, скажете, какой.
— ответ:

## 4. Комната урока

4.1 **Верхняя строка** держит знак, код комнаты, состояние связи, полку источников, приглушённую дверь «Пока нет: учебник» и «Чат» — шесть вещей в одной строке. Огранка внутри одной строки (группы, воздух, вес) или разрешаете две строки: сверху знак, код, состояние и «Чат», ниже полка источников?
— ответ:

4.2 **Приглушённая дверь про учебник** стоит внутри полки источников. Оставить в полке или вынести под неё отдельной строкой (как описано в `ИНТЕРФЕЙС-КАК-ЕСТЬ.md`, пункт 2)? Снимок и описание расходятся.
— ответ:

## 5. Доска

5.1 **Дефект со снимка:** карточка «Эфир не поднялся» налезает на плашку «доски · 1» — текст на тексте. Чиню на листе (плашка листов и карточка отказа не пересекаются). Подтвердите, что это чинится, а не воспроизводится.
— ответ:

5.2 **Вертикальный ярлык «инструменты»** шириной 30 px: цель нажатия меньше 44. Расширить ярлык или держать панель раскрытой всегда?
— ответ:

## 6. Чат занятия

6.1 **Ширина панели.** Сейчас треть холста и не сжимается. Оставить треть, сузить до 380–400 px или дать две ширины (обычная и «шире» кнопкой)?
— ответ:

6.2 **«Отправить» приглушена всегда**, даже когда в поле есть текст — это дефект кода. На листе кнопка живая, когда в поле есть текст. Подтвердите.
— ответ:

6.3 **Две добавки, которых нет в `Chat.tsx`** — нужны ваше слово (скелет заморожен):
- **строка сообщения** под лентой, стоит всегда: в ней «Отправляем» и «Сообщение не ушло. Связь с комнатой прервалась, текст сохранён в поле». Без неё состояния «отправляем» и «отказ» показать нечем, а всплывающее сообщение сдвигает ленту;
- **время у каждой реплики** (12:03, 12:04). В коде рисуется только имя. Время помогает понять, когда спросили, но это новый элемент.
Счётчик сообщений в шапке я добавлял — убрал сам, без него шапка совпадает с продуктом.
— ответ:

## 7. Шрифты и знак

7.1 **Гарнитура не утверждена.** В `tokens.css` записаны SF Pro Display / SF Pro Text / JetBrains Mono, но задание запрещает шрифты из сети. Во всех листах стоит системный стек: SF Pro на маке, Segoe UI на Windows, системный моно. Покупаем гарнитуру (назову, какую предлагаю) или остаёмся на системной?
— ответ:

7.2 **Моно только для чисел, времени, кодов и служебных подписей** — так сейчас во всех листах. Подтверждаете это правило или моно должно уйти совсем?
— ответ:

## 8. Общее

8.1 **Лист «Показ.dc.html»**, сделанный по первой редакции задания, лежит в `docs/дизайн/листы/`. В очередь 1 редакции 2 он не входит. Удалить, оставить в стороне или доработать позже?
— ответ:

8.2 **Панель инструментов доски плавает НА холсте** — ваше решение от 01.09. Правило 3.10 (`ПРАВИЛА-ДИЗАЙНА.md`) прямо запрещает плавающее над холстом: в продукте нажатия не доходили до доски, под курсором оказывалась панель. На листе панель непрозрачная и стоит в углу, но разработчику нужно знать, что правило перекрыто вашим решением — иначе он будет чинить это как дефект.
— ответ:

