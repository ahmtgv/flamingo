# ПЕРЕДАЧА — от дизайна, 31.08–01.09.2026

Всё, что нарисовано на текущий момент, включая незаконченное. Ничего не удалял.
Папка: `docs/дизайн/от-дизайна-31.08/`. В `frontend/src/` не заходил.

**PNG приложить не удалось.** Инструмент предпросмотра в этой сессии перестал отвечать
(таймаут даже на пустой странице), поэтому снять картинки с артбордов я физически не смог —
ни одной. Файлы `.dc.html` самодостаточны: открываются двойным щелчком в браузере, без сети
и внешних шрифтов. Чтобы получить картинку без кода: открыть файл в браузере → ⌘P → «Сохранить
как PDF». Как только предпросмотр оживёт, приложу PNG отдельным заходом.

Опубликованного холста-артефакта нет: все артборды — отдельные файлы, как просили.

---

## Артборды

| Файл | Какой экран | Состояния | Готовность | Откуда взято устройство экрана |
|---|---|---|---|---|
| `Первая страница.dc.html` | Занятие по ссылке (`screens/Enter.tsx`) | наполнено · пусто · загружается · отказ · частичный отказ; кадр 1440/1280; kids | сдан, есть записка | из снимков `docs/дизайн/как-есть/1-первая-страница.png`. Кнопка Flamingo HUB убрана по вашему решению 31.08 |
| `Вход и регистрация.dc.html` | Вход · новая учётная запись · забыли пароль (`screens/Sign.tsx`) | три режима × наполнено · пусто · загружается · отказ · частичный отказ; кадр 1440/1280; kids | сдан, есть записка | из снимков `2-вход.png`, `3-регистрация.png`. **Тексты режима «Забыли пароль» придуманы мной** — в продукте я их не видел |
| `Первая страница — вариант Б.dc.html` | Занятие по ссылке, отклонённая ветка оформления | только «наполнено» | черновик варианта, вы выбрали эту компоновку и попросили доработать оформление | из снимков `1-первая-страница.png` |
| `Flamingo HUB.dc.html` | Источники мира (`screens/Hub.tsx`, `hub/sources.ts`) | только «наполнено»; фильтры по видам живые | вариант на согласовании | из снимков `4-хаб.png` + ваши правки 31.08. **Место под превью источника придумано мной** — в продукте превью нет; правый рельс «ничего не выбрано» удалён по вашему слову; строка частичного отказа удалена по вашему слову |
| `Комната урока.dc.html` | Комната урока (`screens/Room.tsx`, `room/*`) | эфир идёт / не поднялся · сцены Лица·Доска·Показ·Из HUB · чат открыт/закрыт · день/ночь | вариант на согласовании, **не сверен с кодом** | из снимков `6-комната-эфир-не-поднялся.png`. Верхняя строка и пульт воссозданы со снимка: `room/Shelf.tsx`, `screens/Room.tsx`, `room/useEdge.ts`, `room/Stage.tsx` я не читал |
| `Доска.dc.html` | Доска (`board/*`) | холст пустой / написано · превью видно/свёрнуто · день/ночь · инструменты, цвета, толщина живые | вариант на согласовании | из снимка продукта от 01.09, который вы прислали в чат (в папке `как-есть/` его нет) + **сверено с кодом**: `board/Tools.tsx` (состав и порядок инструментов, клавиши, контуры значков), `board/Sheets.tsx` (полоса досок), `room/Faces.tsx` (метка «ведёт занятие») |
| `Чат занятия.dc.html` | Чат внутри урока (`room/Chat.tsx`) | наполнено · пусто · отправляем · отказ; ширина 380/520; день/ночь; поле ввода работает | вариант на согласовании | из снимка `8-чат-в-уроке.png` + сверено с `room/Chat.tsx` (тексты пустого состояния и подпись поля — дословно из кода) |
| `Показ.dc.html` | Показ материала и рисование поверх | идёт · панель показов · показов нет · страница едет · файл не разобрался · пометки не уходят; ведущий/класс; хранение есть/нет | **сделан по ПЕРВОЙ редакции задания, в очередь 1 редакции 2 не входит** | из кода `room/Show.tsx`, `ShowList.tsx`, `shows.ts`, `Ink.tsx`, `InkTools.tsx`. Снимка этого экрана нет |

**Про прошлую итерацию, начистоту.** Ни один артборд не взят из `docs/дизайн/листы/` —
устройство экранов везде из снимков продукта или из кода. Но **язык оформления** (кадр
1440 × 900, набор токенов, приёмы «моно только для чисел и служебных подписей», «состояние —
точка плюс надпись», «одна заливка на экран») взят с `docs/дизайн/листы/Кабинет учителя.dc.html`
— единственного листа, который задание разрешает как образец. Файл `Показ.dc.html` лежал в
папке запрещённых листов, но нарисован не по ним, а по коду.

---

## Новые токены

Ни одного нового имени в `tokens.css` не заведено и ничего не переименовано. Использованы
существующие. Два уточнения:

- **Зелёный переведён на семью «лист»** (`--fl-leaf-*`: 500 `#619b21`, заливка 700 `#4a7a16`,
  рамка 300 `#a8d96a`, фон 50 `#eef4e2`) по вашему решению 31.08. В `tokens.css` эта семья
  описана как детская, а взрослая — «мох». Развилку не решил: либо `--color-go-*` в корне
  переводятся на лист и утверждённый кабинет перекрашивается вместе со всеми, либо продукт
  живёт с двумя зелёными (вопрос 1.4).
- `--color-preview` (`#e9e9e6` днём, `#1a1a1d` ночью) — заведён **только внутри листа доски**,
  чтобы пустая плашка превью была читаемой днём. В `tokens.css` не добавлял. Если решение
  «плашка превью светлая, пока кадра нет» верно, имя надо завести по-настоящему.

---

## Что осталось незаконченным

- **Первая страница** — готова; ждёт вашего слова по строке сообщения (вопрос 1.3) и по HUB в верхнем меню (1.1).
- **Вход и регистрация** — готова; тексты «Забыли пароль» мои, нужна сверка (2.2). Экран «Новый пароль по ссылке» (`NewPass.tsx`) не рисовал.
- **Первая страница — вариант Б** — черновик; из него выросла сдаточная версия, оставляю как след.
- **Flamingo HUB** — только состояние «наполнено». Нет пусто · загружается · отказ · частичный отказ, нет кадра 1280 и kids, нет записки.
- **Комната урока** — только сцены и два состояния эфира. Нет пяти состояний, нет kids, нет записки; **верхняя строка и пульт не сверены с кодом** — по опыту с `Tools.tsx` и `Sheets.tsx` расхождения вероятны (там выяснилось, что «Заливки» в продукте нет, а плашка листов на самом деле «Доска 1 · + доска · Сохранить в файл · Открыть»).
- **Доска** — панель и плашка сверены с кодом, но нет пяти состояний, нет kids и записки. Сцена «написано» нарисована условно: два штриха, рамка и заметка.
- **Чат занятия** — четыре состояния есть, нет частичного отказа, kids и записки.
- **Показ** — сделан по старой редакции задания; не пересматривал после смены задания.
- **Витрина источника** — не начата: `5-витрина.png` совпадает с `4-хаб.png`, рамки источника на нём нет. Нужен снимок или описание словами.
- **Записок `.md`** нет у пяти листов из восьми (есть только у первой страницы и входа).

---

## Что было непонятно, и я решил сам, а не спросил

1. **Частичный отказ на экранах с одним источником.** Правило 6.5б говорит, что он там не нужен, а форма сдачи требует пять состояний. На первой странице взял за частичный отказ отказ камеры и микрофона, при входе — «вошли, но список уроков не пришёл». Это моя трактовка (вынес в вопрос 1.2, но рисовать не стал ждать).
2. **Строка сообщения, которой нет в продукте.** Добавил на первой странице, во входе и в чате: без неё нечем показать «загружается» и «отказ», а всплывающее сообщение сдвигает макет. Вопросы 1.3 и 6.3.
3. **Время у реплик чата.** В `Chat.tsx` рисуется только имя, я добавил время. Вопрос 6.3.
4. **Кадр 1440 × 900** зафиксировал сам — вы ответили «реши сам» на вопрос о кадре.
5. **Шрифты.** Гарнитуру никто не утверждал; поставил системный стек (SF Pro на маке, Segoe UI на Windows, системный моно), потому что задание запрещает шрифты из сети. Вопросы 7.1–7.2.
6. **Панель инструментов на холсте.** Вы велели положить её на поле доски; правило 3.10 это прямо запрещает (в продукте нажатия не доходили до доски). Сделал как вы сказали, панель непрозрачная. Разработчику надо знать, что правило перекрыто вашим решением — вопрос 8.2.
7. **Группировка HUB по видам** — сделал, потом снял: она мешала ровной решётке. Вид остался меткой в карточке и фильтрами сверху.
8. **Имя «adel · вы» под плашкой превью**, а не поверх кадра: в пустом слоте подпись ложилась на подпись заглушки. В продукте она поверх.
9. **Метка ведущего** — в коде `Faces.tsx` она «ведёт занятие», на снимке «ведёт урок». Взял из кода.
10. **Ластик и «Заливка».** На снимке 01.09 я принял иконку за заливку и нарисовал её; после чтения `Tools.tsx` выяснилось, что заливки в продукте нет, а есть ластик. Исправил по коду.

---

## Что рядом в папке

- `ВОПРОСЫ-ДИЗАЙНА.md` — все открытые вопросы по экранам, с местом под ваш ответ.
- `Первая страница — записка.md`, `Вход и регистрация — записка.md` — записки к сданным листам: что изменилось против снимка, тексты состояний словами, соответствие блоков коду.
- `support.js`, `image-slot.js`, `flamingo-bird.svg`, `flamingo-bird-inverse.svg` — служебные файлы, нужны листам; знак взят из `docs/дизайн/знаки/`, не перерисован.
