__pycache__/
*.py[cod]
.venv/
venv/
*.sqlite3
.pytest_cache/
.ruff_cache/
.env
.env.*
!.env.example
.git/
.DS_Store

# ⚠️ BUILD_COMMIT сюда НЕ добавлять: это отпечаток кода, который едет с кодом. Исключив его,
# мы ослепим ворота свежести выката (deploy.sh, шаг 2а) — и не заметим этого, потому что
# они начнут говорить «контейнер не назвал отпечаток» на каждом выкате подряд.
