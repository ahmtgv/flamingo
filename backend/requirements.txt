Django==5.1.4
PyJWT==2.10.1
django-cors-headers==4.6.0
python-dotenv==1.0.1
argon2-cffi==23.1.0
# Часовые пояса: чистый python, чтобы zoneinfo работал и на образе без
# /usr/share/zoneinfo. Без него сервер не отличит Москву от Владивостока.
# 🔴 Не точная версия, а нижняя граница — нарочно: точную я отсюда проверить
# не могу (в сеть с этой машины не хожу), а выдуманный пин в requirements.txt
# хуже отсутствующего. База поясов обновляется несколько раз в год, и брать
# свежую тут правильнее, чем замораживать.
tzdata>=2024.1

# Превью источников хаба: любую чужую картинку надо привести к одному размеру,
# иначе шесть плиток витрины поедут кромками. Нижняя граница, а не точный пин —
# по той же причине, что и у tzdata.
Pillow>=10.4
