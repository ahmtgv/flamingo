Django>=5.0,<5.2
python-dotenv>=1.0
strawberry-graphql[django]>=0.235
strawberry-graphql-django>=0.40
psycopg[binary]>=3.1
PyJWT>=2.8
# redis is an explicit floor pin; it is also pulled transitively by channels-redis (the
# GraphQL-subscriptions Channels layer). (Celery is deferred — no async tasks are built yet.)
redis>=5.0
# А1: фронт на flamingo.plus, API на api.flamingo.plus — разные origin, и без этих
# заголовков браузер зарежет каждый запрос ещё до того, как он уйдёт.
django-cors-headers>=4.4
channels>=4.1
channels-redis>=4.2
uvicorn[standard]>=0.30
boto3>=1.34
# 🔴 daphne — ТОЛЬКО для проверок (промпт 33). `channels.testing` тянет его своим
# `__init__`, а без него нельзя поднять настоящий веб-сокет в тесте. Продукт работает на
# uvicorn и daphne не использует: он тут ради того единственного стыка, на котором молча
# умирали ВСЕ подписки — «наш код ↔ библиотека», через который проходит только живой сокет.
daphne>=4.1
pytest>=8.0
# Подписки — асинхронные; без этого их тесты просто пропускаются.
pytest-asyncio>=0.23
pytest-django>=4.8
moto[s3]>=5.0
ruff>=0.6
black>=24.0
