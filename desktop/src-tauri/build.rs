//! 🔴 УРОК 15.08, записанный там, где его прочтут перед следующей сборкой.
//!
//! **Сборка, зависящая от того, что кто-то не забыл предыдущую команду, однажды соберётся
//! неправильно.** Так и вышло, и стоило дня.
//!
//! `tauri.conf.json` говорил только `frontendDist: ../../frontend/dist` и НЕ говорил, кто это
//! `dist` кладёт. Tauri честно упаковывал то, что там лежало в этот момент, — а лежала
//! ВЕБ-сборка. В ней адрес API относительный (`/graphql/`), потому что сайт и API стоят за
//! одним обратным прокси. Внутри приложения та же строка разрешается в
//! `tauri://localhost/graphql/` — то есть в само приложение, где никакого API нет.
//!
//! Дальше всё выглядело исправно: запрос падал сетевой ошибкой, экран честно писал «нет связи
//! с сервером», сервер был здоров и на тот же запрос из терминала отвечал кодом. В логе сервера
//! при этом **не было ни одного обращения от приложения** — потому что запрос не покидал машину.
//!
//! Починено не проверкой и не инструкцией, а порядком: `beforeBuildCommand` в
//! `tauri.conf.json` собирает фронт в режиме `desktop` сам. Комментарий не может стоять там,
//! где ему место: строгий JSON их не держит, и схема Tauri отвергает лишние ключи (проверено).
//! Поэтому он здесь — в файле, который выполняется при каждой сборке.

fn main() {
    tauri_build::build()
}
